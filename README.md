# recipes
Reusable recipes for the elastic.io integration platform

#HOWTO

All changes need to be done in ```master``` branch, later merged to ```production``` branch.

To merge changes from ```master``` you need to create a pull reuqest, see menue on the right.

New pull request have to be created with ```production``` as base and ```master``` as source for pull request.
